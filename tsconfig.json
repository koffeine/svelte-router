{
	"exclude": [
		"dist"
	],
	"compilerOptions": {
		// Modules
		"module": "nodenext",
		"moduleResolution": "nodenext",
		"types": [],

		// Emit
		"noEmit": true,

		// JavaScript Support
		"allowJs": true,
		"checkJs": true,

		// Editor Support
		"plugins": [ { "name": "typescript-svelte-plugin" } ],

		// Language and Environment
		"target": "esnext",

		// Completeness
		"skipLibCheck": true
	}
}
